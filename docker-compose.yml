services:
  search:
    build: .
    ports:
      - "${STREAMING_CATALOG_PORT:-18797}:18797"
    volumes:
      - ./data:/app/data:ro
    environment:
      - STREAMING_CATALOG_DB=/app/data/catalog.db
      - STREAMING_CATALOG_PORT=18797
    restart: unless-stopped
